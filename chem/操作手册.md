# 化学连接学习内部参考版操作手册

## 1. 目录说明

- `public/`：本地网页程序。
- `data/content.json`：课程、知识网络和 1600 道题目的源数据。
- `data/mysql/chem_web_mysql.sql`：MySQL 8 完整建库脚本，包含建库、建表和数据导入。
- `data/mysql/schema.sql`：仅数据库结构。
- `data/mysql/data.sql`：仅数据导入。
- `data/exam-pdfs/`：高考化学 PDF 原卷/解析缓存，仅作内部参考。
- `scripts/create-mysql-dump.js`：从 `content.json` 和 PDF 清单重新生成 MySQL SQL。
- `启动-内部参考版.bat` / `START_INTERNAL.bat`：启动本地网页。
- `导入MySQL数据库.bat` / `IMPORT_MYSQL.bat`：导入 MySQL 数据库。
- `重建MySQL脚本.bat` / `REBUILD_MYSQL_SQL.bat`：重新生成 MySQL SQL 文件。

## 2. 运行环境

必须安装：

- Node.js 18 或以上版本。
- MySQL Server 8.0 或以上版本。
- MySQL 命令行客户端 `mysql`，并确保 `mysql.exe` 可在命令行中直接运行。

如需重新生成 SQL，还需要保留本包内的 Node.js 脚本文件。

## 3. 启动网页

1. 解压压缩包。
2. 双击 `启动-内部参考版.bat`。如果同事电脑中文文件名显示异常，双击 `START_INTERNAL.bat`。
3. 浏览器打开 `http://localhost:5188/`。

如果窗口提示端口占用，可先关闭已有 Node 窗口，或让技术同事修改 `server.js` 中的端口。

## 4. 创建 MySQL 数据库

默认数据库名：

`chem_web_internal`

导入方式：

1. 确认 MySQL 服务已启动。
2. 双击 `导入MySQL数据库.bat`。如果中文文件名显示异常，双击 `IMPORT_MYSQL.bat`。
3. 根据提示输入 MySQL 用户名，默认为 `root`。
4. 根据 MySQL 客户端提示输入密码。

导入脚本会执行：

`mysql --default-character-set=utf8mb4 -u 用户名 -p < data\mysql\chem_web_mysql.sql`

如需使用不同数据库名，可先设置环境变量后重建 SQL：

`set CHEM_MYSQL_DATABASE=your_database_name`

`npm run mysql:dump`

## 5. MySQL 数据表

- `metadata`：数据统计和版本说明。
- `course_modules`：课程模块。
- `knowledge_edges`：知识网络关系。
- `questions`：题库。
- `exam_pdfs`：本地 PDF 缓存清单。

当前数据规模：

- 题库：1600 道。
- PDF 清单：以 `data/exam-pdfs/manifest.json` 为准。

## 6. 重新生成 MySQL SQL

修改 `data/content.json` 或 `data/exam-pdfs/manifest.json` 后，双击：

`重建MySQL脚本.bat` 或 `REBUILD_MYSQL_SQL.bat`

也可以在命令行运行：

`npm run mysql:dump`

然后再次执行 `导入MySQL数据库.bat`。

## 7. PDF 原卷

PDF 位于：

`data/exam-pdfs/`

清单位于：

`data/exam-pdfs/manifest.json`

这些 PDF 用于内部参考和后续拆题草稿整理，不会自动导入正式题库。

## 8. 内部参考边界

本包已在页面和服务响应中设置不被搜索引擎索引：

- HTML: `noindex,nofollow,noarchive`
- HTTP Header: `X-Robots-Tag: noindex, nofollow, noarchive`

请仅用于内部学习、评审和题库整理，不作为公开商业站点直接发布。

## 9. 常见问题

### 双击启动后打不开

检查是否安装 Node.js：

`node -v`

### 导入 MySQL 失败

检查 MySQL 是否可用：

`mysql --version`

再确认用户名、密码和权限。导入账号需要具备 `CREATE DATABASE`、`CREATE TABLE`、`INSERT`、`DROP` 权限。

### 修改题库后页面没变化

运行：

`node scripts/build-data.js`

然后刷新浏览器。
